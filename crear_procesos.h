#ifndef CREAR_PROCESOS_H
#define CREAR_PROCESOS_H

void crear_sucursal(const *char ciudad, int capacidad);
// creamos la sala en la ciudad que nos pidan con cierta capacidad
void crear_sucursal (char nombresala, int capacidad);
// creamos sala con el nombre que se escriba por pantalla

#endif
